schema_version = 1
purpose = "Implement and verify functionality behind the published interface."
instructions = "Develop against the declared fixtures first. Preserve consumer compatibility and run the relevant checks."
goal = "Complete one assigned task and deliver a validated result with evidence."

[[deliverables]]
id = "result"
path = "output/{agent}/result.json"
format = "application/json"
schema = "schemas/result.schema.json"
description = "A structured result describing the outcome, evidence, and produced artifacts."
